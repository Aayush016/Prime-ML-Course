# Working with APIs\-------------------------------------------------API - Application Programming Interface* Its work is to fetch data from backend.We will use free API's[Free APIs](https://free-apis.github.io/#/)Types of Requests:1. GET2. POST3. DELETEWorking:We put the API in a variable and then call it using requests.get(URL) commandTo convert JSON data to DataFrame we use this command:import pandas as pddf = pd.json\_normalize(json\_data\["data"])# WEB SCRAPING\-------------------------------------------------There are many popular Python libraries for web scraping, the most commonly used ones are:* **Requests** - Used to download the webpage i.e. HTML* **BeautifulSoup** - Parses and extracts data from HTML (Useful for static HTML). We use this and requests together.* Selenium - Automate a browser to load dynamic JS pages - optional in case dynamic website somesBeautifulSoup is a python library used to parse HTML and xml docs. It is helpful in extracting data from webpagesa after downloading them.Methods:1. soup.find(): 1st Matching element {Or we can directly do this -> soup.h1}2. soup.find\_all(): All matching elements3. soup.find\_parent(): Used to find the parent of the tag4. soup.find\_next():5. soup.find\_previous():=> To get the text from the html tags we'll use this method: ***.get\_text(strip=True)***#### Methodscss selectors* select()* select\_one()attributes* get( )* .attrs